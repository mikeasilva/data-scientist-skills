## Credentials.R

# These are the credentials for accessing the MySQL server
mysql_user <- 'user'
mysql_password <- 'password'
mysql_host <- 'host'

# Set your Census API Key
# If you don't have a key get one at https://api.census.gov/data/key_signup.html
Sys.setenv(CENSUS_KEY = "YOUR API KEY")